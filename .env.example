# Copy to .env and set values. Do not commit .env.
# For local PostgreSQL (e.g. docker compose up -d):
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/appdb
SECRET_KEY=your-secret-key-here
